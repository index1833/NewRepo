using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;


public class Test : MonoBehaviour // Class = 함수의 집합 ★★★클래스이름과 파일이름은 동일해야함
{ 
    // Start is called before the first frame update == 초기값넣을때사용, 초기화 
    void Start()
    {
        print("Hello World");
    }

    // Update is called once per frame
    void Update() // 실시간으로 업데이트가 되는상황에서 사용
    {
        
    }
  
}
